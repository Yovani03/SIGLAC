import os
import django

os.environ.setdefault("DJANGO_SETTINGS_MODULE", "core.settings") # Adjust settings module if it's different! Let's check manage.py. Actually I'll use core.settings or backend.settings. Wait, the app is api.
# Let's read manage.py to be sure.
